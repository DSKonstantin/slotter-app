import React from "react";
import ProfileSettings from "@/src/components/app/account/profileSettings";

const ProfileSettingsPage = () => {
  return <ProfileSettings />;
};

export default ProfileSettingsPage;
